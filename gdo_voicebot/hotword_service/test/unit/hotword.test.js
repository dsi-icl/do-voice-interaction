const WakewordDetector = require('@mathquis/node-personal-wakeword')
const hotwordService = require('../../src/routes/hotword')


test('Returns 400 on empty body request', () => {

})

test('Returns 200 on every body containing audio', () => {

})

test('"not-present" message if NO hotword is detected', () => {

})

test('"detected" message if hotword is detected', () => {

})